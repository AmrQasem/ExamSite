@model List<ExamSite.ViewModels.QuestionsVM>
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>To Be Edited Course Page</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <!-- bootstrap css -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <!--Playfair font-->
    <link href="https://fonts.googleapis.com/css?family=Playfair+Display" rel="stylesheet">
    <!-- my css -->
    <link rel="stylesheet" type="text/css" media="screen" href="~/Content/toBeEditedCourse.css" />
    <style>
        p {
            text-align: center;
            font-size: 60px;
            margin-top: 0px;
        }
    </style>
</head>
<body>
    <div class="home">
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <a class="navbar-brand" href="#">
                <img src="~/images/logo.svg" width="30" height="30" class="d-inline-block align-top" alt="">
                Exam Name
            </a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <form class="serch-form form-inline mx-auto my-2 my-lg-0">
                    <input class="search form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
                    <button class="btn btn-primary my-2 my-sm-0" type="submit">Search</button>
                </form>
                <div class="not-signedIn form-inline my-2 my-lg-0">
                    <form class="login-redirect form-inline my-2 my-lg-0">
                        <a href="~/User/Login">
                            <button class="btn btn-success my-2 my-sm-0" type="button">Login</button>
                        </a>
                    </form>
                    <form class="signup-redirect form-inline my-2 my-lg-0">
                        <a href="~/User/Register">
                            <button class="btn btn-danger my-2 my-sm-0" type="button">Sign up</button>
                        </a>
                    </form>
                </div>
            </div>
        </nav>

        <div class="mainPage">
            <div class="container">
                <div class="row">

                    <div class="mx-auto text-center col-xl-4 col-lg-4 col-md-4 col-12">
                      <div class="card mx-auto">
                          <div class="card-body">
                              <div>
                                  <strong>
                                      The Exam Will closes in
                                      <span id="time">10:00</span> minutes!

                                  </strong>
                              </div>

                              <div class="form-group">
                                  <div class="list-group qqqq">
                                      @foreach (var item in Model)
            {
                          <a href="@Url.Action("AddAnswers","Exam", new {QuestID=item.ID })" class="list-group-item active">@item.QuestionTitle</a><br>
}
                                  </div>
                                  @*@foreach (var item in Model)
            {
                <div class="procedures form-group">
                    <label for="toBeEdited">@item.QuestionTitle </label>
                    <select class="form-control" id="toBeEdited">
                        <option disabled selected>Choose what you need to edit</option>
                        <option value="Edit info">AnswerDetails</option>
                    </select>
                </div>
            }*@
                                  @*<a href="editCourseDetails.html">
                <input class="btn btn-success form-control" value="Submitt" type="button" />
            </a>*@
                              </div>
                          </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <script src="https://code.jquery.com/jquery-3.3.1.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js"></script>
    <script src="js/toBeEditedCourse.js"></script>
    <script>
        function startTimer(duration, display) {
            var timer = duration, minutes, seconds;
            setInterval(function () {
                minutes = parseInt(timer / 60, 10)
                seconds = parseInt(timer % 60, 10);

                minutes = minutes < 10 ? "0" + minutes : minutes;
                seconds = seconds < 10 ? "0" + seconds : seconds;

                display.textContent = minutes + ":" + seconds;

                if (--timer < 0) {
                    timer = duration;
                }
            }, 1000);
        }

        window.onload = function () {
            var TenMinutes = 60 * 10,
                display = document.querySelector('#time');
            startTimer(TenMinutes, display);
        };
    </script>

</body>